import type { ThemeConfig } from '../types'

/** Aurora glass: deep indigo, aurora gradients, frosted cards, a glowing app window. */
export default {
  stepMarks: 'digits',
  highlights: null,
  themeColor: { light: '#0b0a1f' },
} satisfies ThemeConfig
