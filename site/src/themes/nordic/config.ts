import type { ThemeConfig } from '../types'

/** Nordic calm: stone and sage, light type with serif italics, a landscape arch. */
export default {
  stepMarks: 'words',
  highlights: null,
  themeColor: { light: '#f1efe9' },
} satisfies ThemeConfig
