import type { ThemeConfig } from '../types'

/** Bold / dark tech: dark grid, mono labels, lime highlights, a terminal hero. */
export default {
  stepMarks: 'code',
  highlights: 'band',
  themeColor: { light: '#0a0b0d' },
} satisfies ThemeConfig
