import type { ThemeConfig } from '../types'

/** Editorial: warm paper, serif display, hairline rules, a pricing ledger. */
export default {
  stepMarks: 'roman',
  highlights: null,
  themeColor: { light: '#f4efe6', dark: '#16140f' },
} satisfies ThemeConfig
