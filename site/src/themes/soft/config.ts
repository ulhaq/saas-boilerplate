import type { ThemeConfig } from '../types'

/** Soft / friendly: cream and pastels, rounded cards, a playful project card. */
export default {
  stepMarks: 'digits',
  highlights: null,
  themeColor: { light: '#fffaf4' },
} satisfies ThemeConfig
