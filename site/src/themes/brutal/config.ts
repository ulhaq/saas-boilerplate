import type { ThemeConfig } from '../types'

/** Neo-brutalist: thick borders, hard shadows, loud colour blocks, a ticker. */
export default {
  stepMarks: 'digits',
  highlights: 'ticker',
  themeColor: { light: '#fff3d6' },
} satisfies ThemeConfig
