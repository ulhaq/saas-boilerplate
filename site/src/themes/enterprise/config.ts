import type { ThemeConfig } from '../types'

/** Enterprise clean: white and navy, centred hero with a product dashboard, trust badges. */
export default {
  stepMarks: 'digits',
  highlights: 'badges',
  themeColor: { light: '#ffffff' },
} satisfies ThemeConfig
