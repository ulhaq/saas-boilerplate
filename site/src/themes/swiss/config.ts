import type { ThemeConfig } from '../types'

/** Swiss / Klein: strict grid, huge grotesk, one electric blue, geometric figure. */
export default {
  stepMarks: 'digits',
  highlights: null,
  themeColor: { light: '#ffffff' },
} satisfies ThemeConfig
